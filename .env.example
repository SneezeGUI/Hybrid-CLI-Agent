# Hybrid CLI Agent Configuration

# =============================================================================
# GEMINI AUTHENTICATION (choose one method)
# =============================================================================

# METHOD 1: OAuth (RECOMMENDED for Pro/Ultra subscribers)
# Run: gemini auth login
# Benefits: 60 RPM, 1000 RPD FREE, no API key needed
# This is the default method if no API keys are set

# METHOD 2: Standard API Key
# Get key: https://makersuite.google.com/app/apikey
# GEMINI_API_KEY=your-gemini-api-key

# METHOD 3: Vertex AI (for Gemini 3 Pro access)
# Best for: Production use, no rate limits, Gemini 3 Pro
# VERTEX_API_KEY=your-vertex-api-key
# VERTEX_PROJECT=your-gcp-project-id
# VERTEX_LOCATION=us-central1

# Alternative: Google API Key (same as GEMINI_API_KEY)
# GOOGLE_API_KEY=your-google-api-key

# =============================================================================
# MODEL CONFIGURATION
# =============================================================================

# Force specific models (otherwise auto-selected based on auth)
# HYBRID_CLAUDE_MODEL=claude-sonnet-4-5-20250514
# HYBRID_GEMINI_MODEL=gemini-2.5-pro

# For Vertex AI users, you can use:
# HYBRID_GEMINI_MODEL=gemini-3-pro

# =============================================================================
# ORCHESTRATOR SETTINGS
# =============================================================================

# Disable Claude review of Gemini's code output
# HYBRID_SKIP_REVIEW=false

# Maximum correction iterations in review loop
# HYBRID_MAX_RETRIES=3

# Working directory (defaults to current directory)
# HYBRID_WORK_DIR=/path/to/project

# =============================================================================
# AGENT MODE (ADVANCED)
# =============================================================================

# Enable Gemini's autonomous agent mode with native file/shell access
# WARNING: This allows Gemini to execute shell commands and modify files directly.
# Only enable in trusted environments where you want Gemini to have full control.
#
# When enabled, the gemini_agent_task tool becomes available:
# - Native file system access (read/write/create)
# - Shell command execution (npm test, git, build, etc.)
# - Session persistence for multi-step tasks
# - Automatic iteration until task complete
#
# GEMINI_AGENT_MODE=false

# =============================================================================
# AUTHENTICATION SETUP GUIDE
# =============================================================================
#
# For FREE usage (recommended):
#   1. Install Gemini CLI: npm i -g @google/gemini-cli
#   2. Authenticate: gemini auth login
#   3. You now have 60 requests/min, 1000 requests/day FREE!
#
# For Gemini 3 Pro access:
#   1. Go to Google Cloud Console
#   2. Enable Vertex AI API
#   3. Create service account and get API key
#   4. Set VERTEX_API_KEY in this file
#
# For Claude Code:
#   1. Install: npm i -g @anthropic-ai/claude-code
#   2. Authenticate: claude login
#   3. Requires Claude Pro subscription ($20/month)

